machine:
  node:
    version: 0.10.33
  pre:
    - curl https://install.meteor.com | /bin/sh
dependencies:
  pre:
    - npm install -g spacejam
    - meteor npm install
    override:
      - mgp
test:
  override:
    - spacejam test --full-app --driver-package practicalmeteor:mocha